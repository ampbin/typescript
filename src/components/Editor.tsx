import { h, Component } from 'preact';

export default class Editor extends Component {
    constructor() {
        super();
    }
    
    render(props, state) {
        return (
            <div>
                Hello
            </div>
        );
    }
}
